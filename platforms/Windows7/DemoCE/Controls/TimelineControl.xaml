<UserControl x:Class="DemoCE.Controls.TimelineControl" x:Name="tlControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:DemoCE.Converters"
             mc:Ignorable="d" Focusable="True"
             d:DesignHeight="230" d:DesignWidth="630" Background="SkyBlue">

	<UserControl.Resources>
		<converters:HeightMarginConverter x:Key="hmConverter"/>
		<converters:NegateBooleanConverter x:Key="nbConverter"/>
	</UserControl.Resources>
	<Grid RenderOptions.EdgeMode="Aliased">
		<Line x:Name="lAxisY" Stroke="Black" StrokeThickness="1.5" HorizontalAlignment="Left" Margin="30,0" X1="0" Y1="0" X2="0" Y2="{Binding ElementName=tlControl, Path=ActualHeight}"/>
		<Line x:Name="lAxisX" Stroke="Black" StrokeThickness="1.5" VerticalAlignment="Bottom" Margin="0,30" X1="0" Y1="0" X2="{Binding ElementName=tlControl, Path=ActualWidth}" Y2="0"/>
		<Button HorizontalAlignment="Right" VerticalAlignment="Top" Height="Auto" Width="25"  Click="BtDeleteClick">
			<Button.Template>
				<ControlTemplate TargetType="Button">
					<Grid>
						<Image Source="/DemoCE;component/CloseButton.png" />
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
					</Grid>
				</ControlTemplate>
			</Button.Template>
		</Button>
		<Button Opacity="0.1" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="70" Height="70" 
						Margin="0,0" MouseEnter="Button_MouseEnter" MouseLeave="Button_MouseLeave" Click="BtReplayClick"
						IsEnabled="{Binding ElementName=tlControl, Path=IsEngineRunning, Converter={StaticResource ResourceKey=nbConverter}}">
			<Button.Template>
				<ControlTemplate TargetType="Button">
					<Grid>
						<Image Source="/DemoCE;component/PlayButton.png" />
						<ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
					</Grid>
				</ControlTemplate>
			</Button.Template>
		</Button>
		<Grid x:Name="gGraphContent" VerticalAlignment="Bottom" Margin="30,0,0,30" HorizontalAlignment="Left"
					Width="{Binding ElementName=tlControl, Path=ActualWidth}" Height="{Binding ElementName=tlControl, Path=ActualHeight}">
			<Canvas x:Name="cGraphContent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible" />
			<Line x:Name="lCEMax" Stroke="Red" StrokeThickness="1.5" StrokeDashArray="3" VerticalAlignment="Bottom" Margin="0,0,0,100" X1="0" Y1="0" X2="{Binding ElementName=tlControl, Path=ActualWidth}" Y2="0" />
			<Label HorizontalAlignment="Left" VerticalAlignment="Bottom" Content="{Binding ElementName=tlControl, Path=MaxValue}" Foreground="Red" Margin="0,0,0,100"/>
			<TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom" Text="100 %" Foreground="Red" Margin="0,0,20,0"/>
		</Grid>
	</Grid>
</UserControl>